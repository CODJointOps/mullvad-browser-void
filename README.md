## Usage

Clone this repo into your `void-packages/srcpkgs` folder with the following commands:

```bash
cd void-packages/srcpkgs
git clone https://git.deadzone.lol/Wizzard/mullvad-browser-void mullvad-browser
```

## Architecture support

Upstream Mullvad Browser release artifacts currently provide Linux binaries only for `x86_64`.
